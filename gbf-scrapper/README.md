# GBF Scrapper
A tool to scrape data off of the wiki.

## Usage
`python3 scrape.py`.

## Schema
The schema for weapons is currently: `(id, name, wep_type, wep_element, ougi, skill_one, skill_two, skill_three)`